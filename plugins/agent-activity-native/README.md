# Agent Activity

Source-owning default provider for application-wide terminal/local-agent
activity, attention routing, and recent notifications.

It consumes public workspace, terminal, desktop, preferences, shortcuts, and
event capabilities. It does not import the workspace shell, terminal session
implementation, header, or AI stores.
