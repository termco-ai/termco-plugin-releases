# Terminal Agent Hooks and Activity Signals

The complete default implementation of `agents.terminal-hooks` plus its
renderer-side lifecycle-signal adapter. Copy this folder to support different
agents, events, config formats, delivery mechanisms, or terminal activity
routing while continuing to publish into the selected shared
`agents.activity-control` provider.
