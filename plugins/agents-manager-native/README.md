# Agents & Snippets Manager

Source-owned workspace view for browsing and editing the shared `ai.library`.
Copy this folder and replace `agents-manager-native` to change the complete UI
without copying the provider, persistence, or MCP connections.
