/**
 * AgentSwitcher public surface. Preserves the exports of the former
 * single-file `components/AgentSwitcher.tsx`: the `AgentSwitcher` component
 * and the `AGENT_ICONS` alias of the icon registry.
 */

export { AgentSwitcher } from "./AgentSwitcher";
;
