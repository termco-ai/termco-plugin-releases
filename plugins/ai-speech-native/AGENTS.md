# Plugin boundary

- Keep credentials inside this provider and return only configuration booleans.
- Route network requests through `network.http` and allow private access only for validated loopback Whisper.cpp URLs.
- Preserve recording bytes, timeout behavior, and WAV conversion tests.
