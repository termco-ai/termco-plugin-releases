# AI Tools: Browser

This plugin owns the browser AI tools and per-session origin approval policy.
It consumes `browser.automation`, so it drives the exact embedded browser views,
logins, CDP sessions, snapshots, and page state already owned by the selected
application-wide provider.
