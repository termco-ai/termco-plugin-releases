# AI Tools: Containers & Ports

This plugin owns the container and SSH port-forward AI tools. It consumes the
selected shared providers, so tool calls operate on the same remote workspace,
container runtime, SSH pool, and forward manager as the rest of the app.
