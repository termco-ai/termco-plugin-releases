# AI Tools: Git

This plugin contributes Git inspection and mutation tools. It consumes the one
selected `git.repository` provider, so local, WSL, and SSH repository behavior
and state stay shared across every consumer.

Copy this folder to change tool descriptions, schemas, approval policy, or the
set of Git operations without copying the Git runtime or AI sessions.
