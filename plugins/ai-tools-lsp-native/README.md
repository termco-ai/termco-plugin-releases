# AI Tools: Language Intelligence

Source-owned read-only LSP tools. Every request uses the selected application-wide `lsp.sessions` provider, so editor and AI share the same server processes and diagnostics cache.
