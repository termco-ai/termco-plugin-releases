# AI Tools: Subagents

This plugin owns the `run_subagent` tool, subagent definitions, prompts,
read-only tool policy, progress reporting, and orchestration. It consumes the
selected application-wide inference provider and reusable file/search toolsets.
