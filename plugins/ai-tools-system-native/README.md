# AI Tools: System

This plugin contributes the system AI tools. It consumes the selected
application-wide desktop and shell-history providers, so notifications,
clipboard state, history, and file-manager behavior remain shared.

Copy this folder to add, remove, or rewrite these tools without copying the
desktop integration, command-history index, or AI session state.
