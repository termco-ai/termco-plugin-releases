# AI Tools: Terminal

This plugin owns private shell, visible-terminal, preview, and tab-navigation AI
tools. It consumes the selected `shell.execution` provider for persistent and
background commands. Visible terminal operations use the session-scoped public
AI tool runtime, which projects the application's existing terminal surface and
PTY instead of opening another one.
