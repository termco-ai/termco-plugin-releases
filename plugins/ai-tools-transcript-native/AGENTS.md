# Plugin boundary

- Own the model-facing recall tool names, schemas, descriptions, and errors.
- Consume storage and transcript data only through `ai.context-artifacts`.
- Keep these tools outside persona groups so compacted context remains reachable.
