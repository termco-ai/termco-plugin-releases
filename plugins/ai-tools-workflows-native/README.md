# AI Tools: Workflows

This plugin contributes workflow discovery, execution, and authoring tools to
the shared AI tool registry. It consumes the selected `workflows.library`
provider, so the AI sees exactly the same workflows as the dock and command
palette.
