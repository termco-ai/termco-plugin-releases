# Appearance Settings Plugin

This folder owns the complete Appearance settings feature. It may consume only
the public `ui.theme` capability and the shared renderer SDK. A fork must be
copyable and replaceable without importing the default theme provider or host
settings source.
