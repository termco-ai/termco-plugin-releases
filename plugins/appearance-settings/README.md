# Appearance Settings

Source-owning consumer plugin for the categorized, searchable Appearance
settings section. The selected `ui.theme` provider owns all state and behavior;
this plugin owns the controls and workflows that mutate it.
