# Command Palette

This folder owns the complete default palette: open state, header anchoring,
categorized command search, explanations, workspace-content search, shell
history, theme selection, keyboard navigation, and empty/error states.

Commands remain independent keyed contributions. Copy this whole folder and
declare it as the replacement to change the palette without editing the shell,
header, or any command provider.
