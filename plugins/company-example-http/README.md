# Example Company HTTP Provider

This deliberately small provider demonstrates how a company profile replaces
an application-wide privileged capability without changing the host. It owns
its implementation and shared runtime, requests main/network permissions, and
can be copied and replaced like any other plugin.
