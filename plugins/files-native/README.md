# Workspace Files

This source-owning provider implements `workspace.files`. It combines the
selected workspace registry and shared SSH runtime to expose one consistent
file/search/watch capability for local, WSL, and remote workspaces.
