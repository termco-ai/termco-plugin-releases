# Protected HTTP Client

This source-owning provider implements `network.http`. It validates targets,
classifies resolved addresses, pins connections to safe IPs, disables redirects,
and owns request/stream lifecycle.
