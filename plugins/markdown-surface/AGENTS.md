# Markdown surface ownership

- This folder owns all Markdown-tab presentation and file-load behavior.
- Read files only through `workspace.files`.
- Preserve inactive pane mounting so scroll state survives tab switches.
