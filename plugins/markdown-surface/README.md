# Markdown Surface

This folder owns the complete rendered-Markdown tab surface. It consumes the
selected workspace filesystem, retains a mounted pane per warm Markdown tab,
and uses the public tab runtime to switch a file back to the raw editor.

Copy the folder to replace rendering, controls, loading states, or styling.
