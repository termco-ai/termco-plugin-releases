# Built-in AI Models

The default `ai.models` registry provider. It owns provider metadata, the full
built-in model catalogue, autocomplete defaults, endpoint conventions,
reasoning metadata, context limits, and pricing. Copy this entire folder to
fork or replace the application's default model registry.
