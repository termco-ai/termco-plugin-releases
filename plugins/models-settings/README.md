# Models Settings

Consumer UI for the `ai.models` registry. It stores durable choices through
`settings.preferences`, secrets through `secrets.application`, tests endpoints
through `network.http`, and opens provider documentation through
`desktop.integration`. It contains no built-in model catalogue of its own.
