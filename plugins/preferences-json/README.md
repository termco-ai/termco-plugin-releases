# Application Preferences

Default provider for `settings.preferences@1`. It stores all preference keys in
the selected application-wide storage provider and publishes changes through
the selected application event bus. Feature plugins own the meaning, defaults,
and validation of the keys they use.
