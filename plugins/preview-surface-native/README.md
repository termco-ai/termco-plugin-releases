# Web Preview

Complete source-owning embedded-browser feature. It consumes the selected
application-wide `browser.automation` provider, so preview tabs and AI browser
tools share the same sessions, cookies, state, console, and network history.

Copy this whole folder to replace or customize the preview UI and workflows.
The native browser pool remains replaceable independently through its provider.
