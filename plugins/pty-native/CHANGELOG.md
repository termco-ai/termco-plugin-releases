# Changelog

## 1.0.1

- Reuse saved SSH passwords in terminal tabs and release pending authentication when a terminal closes.

- Wait for native terminal exit callbacks during application shutdown so macOS updates can install without reporting that Termco closed unexpectedly.
