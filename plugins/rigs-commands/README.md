# Rig Commands

This source-owning plugin provides the dynamic Rigs group in the command
palette. It consumes the shared rig provider for selection/cycling and the
header-owned rig-overview capability for the management interface. The shell
does not own command names, keywords, grouping, workflows, or overview state.
