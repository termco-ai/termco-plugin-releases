# Plugin seam

- Keep the selection popup, capture policy, and behavior tests in this folder.
- Import lifecycle types from `@termco/kernel` and session contracts and
  constants from their owning `@termco/*-base` packages.
- Import renderer primitives from `@termco/ui`.
