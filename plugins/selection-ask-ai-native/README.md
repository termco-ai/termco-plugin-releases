# Ask AI from Selection

This plugin owns the exact editor/terminal selection popup and routes the
selected text through the application-wide AI session capability. Copy the
whole folder to replace or modify this workflow without editing the shell,
terminal, editor, or chat plugins.
