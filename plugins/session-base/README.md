# @termco/session-base

Public, provider-independent contracts for Termco's authoritative typed session history.

This package owns session identity, format metadata, structured errors, event and projection contracts, and pure validation/folding behavior. It does not own persistence, Electron transport, model execution, Chat state, or UI.
