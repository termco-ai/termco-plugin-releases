# Application Shortcuts Provider

This folder owns the complete default shortcut catalog and runtime binding
policy. It may consume only public capabilities. A replacement can be made by
copying this whole folder, editing `src/model.ts`, and selecting the copy as the
application-wide `shortcuts.registry` provider.
