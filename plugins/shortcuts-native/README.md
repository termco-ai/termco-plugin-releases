# Application Shortcuts

The selected renderer-wide shortcut provider. It owns definitions, grouping,
matching, platform labels, user overrides, and persistence. Consumers request
`shortcuts.registry`; they never import this plugin's private source.
