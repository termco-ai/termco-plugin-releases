# Shortcut Settings Plugin

This folder owns the complete searchable shortcut settings UI. It consumes the
public application-wide shortcut registry and may not import the default
provider or host source.
