# Sidebar Navigation

Default provider for `ui.sidebar-navigation@1`. It preserves the established
sidebar selection, command-toggle, collapse, sizing, and persistence behavior
without depending on a particular header or sidebar-view implementation.
