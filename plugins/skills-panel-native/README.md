# Agent Config Library

Source-owned sidebar for discovering and adopting workspace AI configuration.
It consumes the shared `ai.library` provider, so adopted items immediately
appear in Agents Manager and AI Chat without creating duplicate stores or MCP
connections.
