# Storage Renderer Bridge

This bridge projects storage-family services from the selected main-process
providers into the renderer through the product-neutral kernel transport.

The storage family owns its method and event serialization. The generic host
does not name storage services or preference methods.
