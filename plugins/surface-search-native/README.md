# Surface Search Registry

Default provider for `ui.surface-search@1`. It owns the application-wide map
from mounted tab IDs to their source plugin's search handle, including
split-pane isolation and identity-safe cleanup during remount or replacement.
