# Terminal AI Workspace Footer

Owns the integration between the selected terminal block-input footer and the
selected AI workspace composer. The terminal and AI providers remain reusable
and independently replaceable; this plugin can be copied to change how they are
combined.
