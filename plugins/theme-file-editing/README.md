# Theme File Editing

This folder owns the complete edit-as-a-file workflow for custom themes. It
subscribes to public application events, uses the selected shared filesystem
and theme providers, and contributes one headless renderer task.

Copy the whole folder to change the file format, location, validation response,
or open/apply behavior without editing the application kernel.
