# Application Theme Provider

This folder owns the complete application-wide appearance runtime. Consumers
use only `ui.theme`. A fork may replace the catalog, persistence policy, CSS
application, background processing, or editor-theme mapping by copying this
whole folder.
