# Application Theme

Selected `ui.theme` provider. It owns built-in and custom themes, mode
resolution, preference persistence, CSS variables, background blobs, and
lifecycle cleanup. The Appearance settings UI is a separate consumer plugin.
