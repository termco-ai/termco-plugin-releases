# Trajectory contract boundary

- Keep this module presentation-only.
- Use current-format session identities from `@termco/session-base`.
- Do not add history reads, writes, sequence allocation, fork, rerun, repair, or compatibility behavior.
