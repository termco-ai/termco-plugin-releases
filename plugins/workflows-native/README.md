# Workflows

This folder is the complete default Workflows feature. It owns the built-in
catalogue, persisted user workflows, favourites, recent values, rendering
rules, authoring UI, searchable commands, dock view, and run sheet.

It shares storage, events, Git, containers, and SSH through public capabilities.
Copy this whole folder and replace `workflows-native` in a profile to fork the
feature without editing application modules.
