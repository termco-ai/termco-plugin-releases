# Workspace Rig Workflows

Default provider for `workspace.rig-workflows@1`. It coordinates rig creation
through the selected environment, rig, tab, and terminal providers so every UI
consumer uses the same behavior without sharing private source.
