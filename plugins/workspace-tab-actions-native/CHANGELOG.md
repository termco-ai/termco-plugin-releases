# Changelog

## 1.0.1

- Open a working terminal directly below a file, starting in the file's directory.
- Keep the file tab pinned and focus the new terminal while the editor stays above it.
- Place the new terminal below the file even when the previous split used a reversed pane order.
