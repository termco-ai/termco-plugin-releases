# Workspace Tab Actions

Default provider for `workspace.tab-actions@1`. It composes kind-owned close
guards and performs compound tab workflows through the selected shared tab and
terminal-session providers.
